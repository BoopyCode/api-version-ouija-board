# API Version Ouija Board

When API documentation is outdated, conflicting, or non-existent, consult the spirits. This tool brute-forces API versions and methods to find what actually works. Run: `python api_ouija.py <base_url> <endpoint>`